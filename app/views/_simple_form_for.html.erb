<%= simple_form_for [@restaurant, @review, html: { class: 'form-inline'}] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :address %>
  <%= f.text_field :address %>

  <%= f.submit %>
<% end %>

<!--   = simple_form_for(@user, html: { class: 'form-inline' }) do |form|
 --> -->
